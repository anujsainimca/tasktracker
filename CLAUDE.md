# TaskTracker API

Simple Flask REST API for managing tasks.
Teaching project for Week 6 — Git, GitHub & CI/CD with Claude Code.

## Structure
- app/routes.py — Flask routes
- tests/test_routes.py — pytest tests
- requirements.txt — dependencies

## Standards
- All routes return JSON
- Tests required for every new route
- No hardcoded secrets
